# Memory — March 9, 2026

## Session Notes

### BAB Scheduler — Morning Session

- Jake asked about the scheduling app we started last night (Mar 8)
- App is at: `/Users/openclaw/.openclaw/workspace/projects/bab-scheduler`
- Stack: Node.js + Express + SQLite (better-sqlite3) + Nodemailer + node-cron
- Full feature set: class schedule, booking, cancellation, waitlist, member lookup, admin dashboard, email reminders

### Issues Fixed Today

1. **503 Tunnel Unavailable** — localtunnel wasn't running. Server was stopped too.
   - Restarted server: `node server.js` (runs on port 3000)
   - Installed localtunnel globally: `npm install -g localtunnel`
   - Jake runs tunnel manually: `lt --port 3000 --subdomain orange-coats-refuse`

2. **Buttons not responding** — Root cause: `openBooking()` in index.html called `document.getElementById('modal-date')` but that element didn't exist in the modal HTML. JS crashed silently before `modal.style.display = 'flex'` could run.
   - Fix: Added `<p id="modal-date">` to the booking modal in index.html ✅

### Current State (end of session)

- ✅ Server running (session: quiet-bloom, pid 41027)
- ✅ Booking works end-to-end
- ✅ My Bookings lookup works by email
- ✅ Waitlist logic in place
- ❌ Email not yet configured (SMTP setup pending)

### Email Setup (pending — Jake to do)

- Go to http://localhost:3000/setup.html
- SMTP: smtp.gmail.com, port 587
- Need Gmail App Password (myaccount.google.com → Security → App Passwords)
- One-time setup — all future customer emails auto-send

### Tunnel Note

- localtunnel subdomain `orange-coats-refuse` is NOT persistent — Jake has to run `lt --port 3000 --subdomain orange-coats-refuse` each session
- Consider switching to ngrok with a reserved domain, or setting up a proper domain + nginx long-term

### Next Steps

- [ ] Jake to configure SMTP on setup page
- [ ] Add admin password protection before going public
- [ ] Consider persistent tunnel or proper domain
- [ ] Admin UI for adding/editing classes (not yet built)
